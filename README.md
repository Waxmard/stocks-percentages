# Create your personal ETF


## Setup and Usage

Install dependencies:

```sh
pip install -r requirements.txt
```

Create and set a `.env` file to match your preferences, use `.env.example` as a starting point.

```sh
python main.py
```
